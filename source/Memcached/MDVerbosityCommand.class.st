"
A MDVerbosityCommand is a command that sets the verbosity level of the logging output.

Instance Variables
	level:		<anInteger>

level
	- the verbosity level

"
Class {
	#name : #MDVerbosityCommand,
	#superclass : #MDCommand,
	#instVars : [
		'level'
	],
	#category : #'Memcached-Commands'
}
