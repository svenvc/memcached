"
Means some sort of client error in the input line, i.e. the input doesn't conform to the protocol in some way.

"
Class {
	#name : #MDClientError,
	#superclass : #MDError,
	#category : #'Memcached-Exceptions'
}
