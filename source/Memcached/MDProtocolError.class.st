"
Means some sort of server error in the output line, i.e. the output doesn't conform to the protocol in some way.
"
Class {
	#name : #MDProtocolError,
	#superclass : #MDError,
	#category : #'Memcached-Exceptions'
}
